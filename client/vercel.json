{
  "version": 2,
  "buildCommand": "CI=false npm install --legacy-peer-deps && CI=false npm run build",
  "installCommand": "CI=false npm install --legacy-peer-deps",
  "framework": "create-react-app",
  "outputDirectory": "build",
  "routes": [
    {
      "src": "/static/(.*)",
      "dest": "/static/$1"
    },
    {
      "src": "/favicon.ico",
      "dest": "/favicon.ico"
    },
    {
      "src": "/manifest.json",
      "dest": "/manifest.json"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ],
  "github": {
    "silent": true
  }
}
